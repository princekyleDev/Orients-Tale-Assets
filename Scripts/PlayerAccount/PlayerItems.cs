using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerItems : MonoBehaviour
{

    /**
    public void UpdateEquippedItems()
    {
        EquippedWeapon();
    }

    public void EquippedWeapon()
    {
        switch ("currentWeapon")
        {
            case "WornBakunawa":
                PlayerAccount.currentWeapon = "Worn Bakunawa";
                PlayerAccount.damage = 1;
                break;

            default:
                Debug.Log("PlayerAccount.EquppedWeapon.NotFound!");
                break;
        }
    }
    **/
}
